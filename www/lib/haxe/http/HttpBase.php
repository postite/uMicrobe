<?php
/**
 * Generated by Haxe 4.0.0 (git build development @ da28365)
 * Haxe source file: /usr/local/lib/haxe/std/haxe/http/HttpBase.hx
 */

namespace haxe\http;

use \php\_Boot\HxClosure;
use \php\Boot;
use \php\_Boot\HxException;
use \php\_Boot\HxAnon;

/**
 * This class can be used to handle Http requests consistently across
 * platforms. There are two intended usages:
 * - call `haxe.Http.requestUrl(url)` and receive the result as a `String`
 * (not available on flash)
 * - create a `new haxe.Http(url)`, register your callbacks for `onData`,
 * `onError` and `onStatus`, then call `request()`.
 */
class HttpBase {
	/**
	 * @var \Array_hx
	 */
	public $headers;
	/**
	 * @var \Closure
	 * This method is called upon a successful request, with `data` containing
	 * the result String.
	 * The intended usage is to bind it to a custom function:
	 * `httpInstance.onData = function(data) { // handle result }`
	 */
	public $onData;
	/**
	 * @var \Closure
	 * This method is called upon a request error, with `msg` containing the
	 * error description.
	 * The intended usage is to bind it to a custom function:
	 * `httpInstance.onError = function(msg) { // handle error }`
	 */
	public $onError;
	/**
	 * @var \Closure
	 * This method is called upon a Http status change, with `status` being the
	 * new status.
	 * The intended usage is to bind it to a custom function:
	 * `httpInstance.onStatus = function(status) { // handle status }`
	 */
	public $onStatus;
	/**
	 * @var \Array_hx
	 */
	public $params;
	/**
	 * @var string
	 */
	public $postData;
	/**
	 * @var string
	 */
	public $responseData;
	/**
	 * @var string
	 * The url of `this` request. It is used only by the `request()` method and
	 * can be changed in order to send the same request to different target
	 * Urls.
	 */
	public $url;


	/**
	 * Creates a new Http instance with `url` as parameter.
	 * This does not do a request until `request()` is called.
	 * If `url` is null, the field url must be set to a value before making the
	 * call to `request()`, or the result is unspecified.
	 * (Php) Https (SSL) connections are allowed only if the OpenSSL extension
	 * is enabled.
	 * 
	 * @param string $url
	 * 
	 * @return void
	 */
	public function __construct ($url) {
		if (!$this->__hx__default__onData) {
			$this->__hx__default__onData = new HxClosure($this, 'onData');
			if ($this->onData === null) $this->onData = $this->__hx__default__onData;
		}
		if (!$this->__hx__default__onError) {
			$this->__hx__default__onError = new HxClosure($this, 'onError');
			if ($this->onError === null) $this->onError = $this->__hx__default__onError;
		}
		if (!$this->__hx__default__onStatus) {
			$this->__hx__default__onStatus = new HxClosure($this, 'onStatus');
			if ($this->onStatus === null) $this->onStatus = $this->__hx__default__onStatus;
		}
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:63: characters 3-17
		$this->url = $url;
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:64: characters 3-15
		$this->headers = new \Array_hx();
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:65: characters 3-14
		$this->params = new \Array_hx();
	}


	/**
	 * @param string $header
	 * @param string $value
	 * 
	 * @return HttpBase
	 */
	public function addHeader ($header, $value) {
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:87: characters 3-45
		$_this = $this->headers;
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:87: characters 3-45
		$_this->arr[$_this->length] = new HxAnon([
			"name" => $header,
			"value" => $value,
		]);
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:87: characters 3-45
		++$_this->length;

		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:88: characters 3-14
		return $this;
	}


	/**
	 * @param string $name
	 * @param string $value
	 * 
	 * @return HttpBase
	 */
	public function addParameter ($name, $value) {
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:110: characters 3-44
		$_this = $this->params;
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:110: characters 3-44
		$_this->arr[$_this->length] = new HxAnon([
			"name" => $name,
			"value" => $value,
		]);
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:110: characters 3-44
		++$_this->length;

		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:111: characters 3-14
		return $this;
	}


	/**
	 * This method is called upon a successful request, with `data` containing
	 * the result String.
	 * The intended usage is to bind it to a custom function:
	 * `httpInstance.onData = function(data) { // handle result }`
	 * 
	 * @param string $data
	 * 
	 * @return void
	 */
	public function onData ($data)
	{
		if ($this->onData !== $this->__hx__default__onData) return call_user_func_array($this->onData, func_get_args());
			}
	protected $__hx__default__onData;


	/**
	 * This method is called upon a request error, with `msg` containing the
	 * error description.
	 * The intended usage is to bind it to a custom function:
	 * `httpInstance.onError = function(msg) { // handle error }`
	 * 
	 * @param string $msg
	 * 
	 * @return void
	 */
	public function onError ($msg)
	{
		if ($this->onError !== $this->__hx__default__onError) return call_user_func_array($this->onError, func_get_args());
			}
	protected $__hx__default__onError;


	/**
	 * This method is called upon a Http status change, with `status` being the
	 * new status.
	 * The intended usage is to bind it to a custom function:
	 * `httpInstance.onStatus = function(status) { // handle status }`
	 * 
	 * @param int $status
	 * 
	 * @return void
	 */
	public function onStatus ($status)
	{
		if ($this->onStatus !== $this->__hx__default__onStatus) return call_user_func_array($this->onStatus, func_get_args());
			}
	protected $__hx__default__onStatus;


	/**
	 * Sends `this` Http request to the Url specified by `this.url`.
	 * If `post` is true, the request is sent as POST request, otherwise it is
	 * sent as GET request.
	 * Depending on the outcome of the request, this method calls the
	 * `onStatus()`, `onError()` or `onData()` callback functions.
	 * If `this.url` is null, the result is unspecified.
	 * If `this.url` is an invalid or inaccessible Url, the `onError()` callback
	 * function is called.
	 * [js] If `this.async` is false, the callback functions are called before
	 * this method returns.
	 * 
	 * @param bool $post
	 * 
	 * @return void
	 */
	public function request ($post = null) {
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:147: characters 3-8
		throw new HxException("not implemented");
	}


	/**
	 * Sets the header identified as `header` to value `value`.
	 * If `header` or `value` are null, the result is unspecified.
	 * This method provides a fluent interface.
	 * 
	 * @param string $name
	 * @param string $value
	 * 
	 * @return HttpBase
	 */
	public function setHeader ($name, $value) {
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:76: lines 76-81
		$_g1 = 0;
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:76: lines 76-81
		$_g = $this->headers->length;
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:76: lines 76-81
		while ($_g1 < $_g) {
			#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:76: lines 76-81
			$_g1 = $_g1 + 1;
			#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:76: characters 8-9
			$i = $_g1 - 1;
			#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:77: lines 77-80
			if (($this->headers->arr[$i] ?? null)->name === $name) {
				#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:78: characters 5-46
				$this->headers[$i] = new HxAnon([
					"name" => $name,
					"value" => $value,
				]);
				#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:79: characters 5-16
				return $this;
			}
		}

		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:82: characters 3-45
		$_this = $this->headers;
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:82: characters 3-45
		$_this->arr[$_this->length] = new HxAnon([
			"name" => $name,
			"value" => $value,
		]);
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:82: characters 3-45
		++$_this->length;

		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:83: characters 3-14
		return $this;
	}


	/**
	 * Sets the parameter identified as `param` to value `value`.
	 * If `header` or `value` are null, the result is unspecified.
	 * This method provides a fluent interface.
	 * 
	 * @param string $name
	 * @param string $value
	 * 
	 * @return HttpBase
	 */
	public function setParameter ($name, $value) {
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:99: lines 99-104
		$_g1 = 0;
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:99: lines 99-104
		$_g = $this->params->length;
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:99: lines 99-104
		while ($_g1 < $_g) {
			#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:99: lines 99-104
			$_g1 = $_g1 + 1;
			#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:99: characters 8-9
			$i = $_g1 - 1;
			#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:100: lines 100-103
			if (($this->params->arr[$i] ?? null)->name === $name) {
				#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:101: characters 5-45
				$this->params[$i] = new HxAnon([
					"name" => $name,
					"value" => $value,
				]);
				#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:102: characters 5-16
				return $this;
			}
		}

		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:105: characters 3-44
		$_this = $this->params;
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:105: characters 3-44
		$_this->arr[$_this->length] = new HxAnon([
			"name" => $name,
			"value" => $value,
		]);
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:105: characters 3-44
		++$_this->length;

		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:106: characters 3-14
		return $this;
	}


	/**
	 * Sets the post data of `this` Http request to `data`.
	 * There can only be one post data per request. Subsequent calls overwrite
	 * the previously set value.
	 * If `data` is null, the post data is considered to be absent.
	 * This method provides a fluent interface.
	 * 
	 * @param string $data
	 * 
	 * @return HttpBase
	 */
	public function setPostData ($data) {
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:125: characters 3-18
		$this->postData = $data;
		#/usr/local/lib/haxe/std/haxe/http/HttpBase.hx:126: characters 3-14
		return $this;
	}
}


Boot::registerClass(HttpBase::class, 'haxe.http.HttpBase');
