<?php
/**
 * Generated by Haxe 4.0.0 (git build development @ da28365)
 * Haxe source file: /usr/local/lib/haxe/std/php/_std/StringBuf.hx
 */

use \php\Boot;
use \php\_Boot\HxString;

/**
 * A String buffer is an efficient way to build a big string by appending small
 * elements together.
 * Its cross-platform implementation uses String concatenation internally, but
 * StringBuf may be optimized for different targets.
 * Unlike String, an instance of StringBuf is not immutable in the sense that
 * it can be passed as argument to functions which modify it by appending more
 * values. However, the internal buffer cannot be modified.
 */
class StringBuf {
	/**
	 * @var string
	 */
	public $b;


	/**
	 * Creates a new StringBuf instance.
	 * This may involve initialization of the internal buffer.
	 * 
	 * @return void
	 */
	public function __construct () {
		#/usr/local/lib/haxe/std/php/_std/StringBuf.hx:32: characters 3-9
		$this->b = "";
	}


	/**
	 * Appends the representation of `x` to `this` StringBuf.
	 * The exact representation of `x` may vary per platform. To get more
	 * consistent behavior, this function should be called with
	 * Std.string(x).
	 * If `x` is null, the String "null" is appended.
	 * 
	 * @param mixed $x
	 * 
	 * @return void
	 */
	public function add ($x) {
		#/usr/local/lib/haxe/std/php/_std/StringBuf.hx:40: lines 40-48
		if ($x === null) {
			#/usr/local/lib/haxe/std/php/_std/StringBuf.hx:41: characters 4-32
			$this->b = ($this->b . "null");
		} else if (is_bool($x)) {
			#/usr/local/lib/haxe/std/php/_std/StringBuf.hx:43: characters 4-58
			$this->b = ($this->b . ($x ? "true" : "false"));
		} else if (is_string($x)) {
			#/usr/local/lib/haxe/std/php/_std/StringBuf.hx:45: characters 4-32
			$this->b = ($this->b . $x);
		} else {
			#/usr/local/lib/haxe/std/php/_std/StringBuf.hx:47: characters 4-5
			$tmp = $this;
			#/usr/local/lib/haxe/std/php/_std/StringBuf.hx:47: characters 4-10
			$tmp->b = ($tmp->b??'null') . (\Std::string($x)??'null');
		}
	}


	/**
	 * Appends the character identified by `c` to `this` StringBuf.
	 * If `c` is negative or has another invalid value, the result is
	 * unspecified.
	 * 
	 * @param int $c
	 * 
	 * @return void
	 */
	public function addChar ($c) {
		#/usr/local/lib/haxe/std/php/_std/StringBuf.hx:56: characters 3-4
		$tmp = $this;
		#/usr/local/lib/haxe/std/php/_std/StringBuf.hx:56: characters 3-30
		$tmp->b = ($tmp->b??'null') . (chr($c)??'null');
	}


	/**
	 * Appends a substring of `s` to `this` StringBuf.
	 * This function expects `pos` and `len` to describe a valid substring of
	 * `s`, or else the result is unspecified. To get more robust behavior,
	 * `this.add(s.substr(pos,len))` can be used instead.
	 * If `s` or `pos` are null, the result is unspecified.
	 * If `len` is omitted or null, the substring ranges from `pos` to the end
	 * of `s`.
	 * 
	 * @param string $s
	 * @param int $pos
	 * @param int $len
	 * 
	 * @return void
	 */
	public function addSub ($s, $pos, $len = null) {
		#/usr/local/lib/haxe/std/php/_std/StringBuf.hx:52: characters 3-4
		$tmp = $this;
		#/usr/local/lib/haxe/std/php/_std/StringBuf.hx:52: characters 3-25
		$tmp->b = ($tmp->b??'null') . (HxString::substr($s, $pos, $len)??'null');
	}


	/**
	 * @return int
	 */
	public function get_length () {
		#/usr/local/lib/haxe/std/php/_std/StringBuf.hx:36: characters 3-18
		return strlen($this->b);
	}


	/**
	 * Returns the content of `this` StringBuf as String.
	 * The buffer is not emptied by this operation.
	 * 
	 * @return string
	 */
	public function toString () {
		#/usr/local/lib/haxe/std/php/_std/StringBuf.hx:60: characters 3-11
		return $this->b;
	}


	public function __toString() {
		return $this->toString();
	}
}


Boot::registerClass(StringBuf::class, 'StringBuf');
Boot::registerGetters('\\StringBuf', [
	'length' => true
]);
